import type { DeclutterSettings } from './types'

export const DEFAULTS: DeclutterSettings = {
	avatarDecorations: true,
	nameplates: true,
	profileEffects: true,
	profileFrames: true,
	serverTags: true,
	badges: true,
}
